import scimate.gui as gui

if __name__ == "__main__":
    gui.main()


    test_folder = "tests/"
    test_file = "out4.txt"

    # df = pd.read_table(test_folder + test_file, comment = "#")
    # df_out,i = mapt.test_dataset(df,.04,mapt.pol2)
    # print(i)
    # fig,ax = plt.subplots()
    # mapt.sliderplot(df_out,ax)
    # plt.show()

    # --- Example DataFrame (replace with your real one) ---
